apiVersion: v2
name: ingestion-service
description: Accepts transactions (single + CSV bulk) and publishes them to Kafka
type: application
version: 0.1.0
appVersion: "0.0.3"
dependencies:
  - name: common
    version: 0.1.0
    repository: "file://../common"
