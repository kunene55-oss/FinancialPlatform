server:
  port: 8888

spring:
  application:
    name: config-server
  security:
    user:
      name: ${CONFIG_SERVER_USER}
      password: ${CONFIG_SERVER_PASSWORD}
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config

  profiles:
    active: native