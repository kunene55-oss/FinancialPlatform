spring:
  application:
    name: processing-service

  config:
    import:
      - optional:configserver:http://configserver:8888
      - vault://

  cloud:
    vault:
      host: vault
      port: 8200
      scheme: http
      authentication: TOKEN
      token: root
